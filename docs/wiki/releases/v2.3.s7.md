# Release V2.3.S7

Date: 2026-05-26

## Scope

`C5 Optimizer setup + budget/epoch controls` completed as a vertical slice.

## Delivered

1. Backend C5 state in arena registry (`optimizer_studio`).
2. API endpoints:
   - `GET /api/arenas/{id}/optimizer/state`
   - `POST /api/arenas/{id}/optimizer/save`
   - `POST /api/arenas/{id}/optimizer/validate`
   - `POST /api/arenas/{id}/optimizer/save-version`
   - `POST /api/arenas/{id}/optimizer/launch`
3. Guardrails for launch readiness:
   - C2 selected candidates + compile gate ready,
   - C4 assigned datasets,
   - C4 evaluation profile status,
   - valid C5 run-plan and budget,
   - at least one enabled optimizer method.
4. Frontend C5 screen:
   - methods/controls checkboxes,
   - run-plan and budget fields,
   - `Save setup`, `Validate`, `Launch`, `Save profile version`,
   - launch queue preview.
5. Tests updated:
   - frontend unit UI flow,
   - backend contract status,
   - integration API path,
   - e2e smoke flow.

## QA Commands

1. `npm run test -- --run src/__tests__/app.workspace.test.tsx`
2. `python -m pytest tests/unit/test_frontend_contracts.py -q`
3. `python -m pytest tests/integration/test_frontend_dev_server.py -q`
4. `python -m pytest tests/e2e/test_frontend_shell_smoke_script.py -q`

## Next

`V2.3.S8` — C5 Run Monitor + version manifest timeline.
